query Sessions($limit: Int!, $cursor: String) {
  sessions(limit: $limit, cursor: $cursor) {
    hasMore
    sessions {
      id
      title
      textSnippet
      start
      end
      attendeeLimit
      creatorId
      createdAt
      updatedAt
    }
  }
}
