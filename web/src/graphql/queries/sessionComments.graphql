query SessionComments($sessionId: Int!) {
  sessionComments(sessionId: $sessionId) {
    id
    text
    sessionId
    creatorId
    creator {
      ...BasicActor
    }
    createdAt
    updatedAt
  }
}
