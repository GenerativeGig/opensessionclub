mutation Login($nameOrEmail: String!, $password: String!) {
  login(nameOrEmail: $nameOrEmail, password: $password) {
    errors {
      field
      message
    }
    actor {
      ...BasicActor
    }
  }
}
